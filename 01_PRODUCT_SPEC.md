# Obsidian Feed V1 产品规格

## 1. 产品定义

### 1.1 一句话

Obsidian Feed 是一个以**长文阅读体验**为中心的 Obsidian 社区插件与自托管内容服务，让用户在同一个安静的阅读环境里订阅 RSS / Atom 和微信公众号文章，并把真正值得保留的内容沉淀成普通 Markdown。

### 1.2 产品不是

它不是：

- 信息推荐平台；
- AI 总结工具；
- 微信公众号镜像站；
- 大型 RSS 管理后台；
- 社交阅读产品；
- 云笔记服务。

## 2. 用户问题

用户的信息来源分散在 RSS 与微信公众号中：

1. 微信公众号阅读页样式不统一，关注、广告、推荐、二维码等元素会干扰长文阅读；
2. RSS 阅读器与 Obsidian 知识库割裂，读完后还要手动搬运；
3. 手机上想读，电脑上想继续读，但用户并不一定需要一个新的云账号体系；
4. 有价值的文章可能被删除、图片失效；
5. 用户只需要“订阅、读、留”，不希望工具不断加入推荐与 AI 噪音。

## 3. 产品原则

按优先级排序：

1. **Reading First**：阅读页永远优先于订阅管理功能；
2. **Content over Chrome**：尽量把屏幕留给正文；
3. **Deterministic**：正文清理以确定性解析器为核心，不依赖模型输出；
4. **Local Knowledge**：用户主动保存的成果必须是普通 Markdown；
5. **Replaceable Sources**：微信采集方式必须可替换；
6. **Mobile First Enough**：桌面好用不代表完成，iOS/Android 必须是正式支持面；
7. **No Lock-in**：即使插件和服务器消失，保存过的 Markdown 仍然可读；
8. **Private by Default**：优先个人自托管，不建设公开全文聚合站。

## 4. 目标用户

V1 目标：

- 已经使用 Obsidian；
- 订阅 5–100 个 RSS/微信公众号来源；
- 每天阅读中文长文、技术文章、行业文章；
- 愿意自托管一个 Docker 服务，或使用自己信任的私有实例；
- 需要把部分文章沉淀到 Vault。

V1 不优化：

- 几万用户公共 SaaS；
- 企业多租户；
- 新闻推荐；
- 大规模全文搜索平台。

## 5. 核心用户旅程

### 5.1 添加普通 RSS

```text
订阅页 → + → 粘贴 RSS/Atom URL → 自动识别 → 显示源信息 → 订阅
```

成功结果：来源出现在“订阅”页，文章进入“今天”。

### 5.2 添加微信公众号

```text
微信打开任意公众号文章
→ 复制链接
→ Obsidian Feed / 添加订阅
→ 粘贴链接
→ 服务端识别公众号
→ 展示公众号名称/头像
→ 确认订阅
→ 后台同步文章
```

用户不需要知道 `biz`、`faker_id`、Feed ID、RSS URL。

### 5.3 阅读

```text
今天 → 点击文章 → 阅读页 → 自动记忆位置 → 读到底部 → 标记已读
```

阅读页只显示：标题、来源、日期、正文和必要媒体。

### 5.4 保存

```text
阅读页 ··· → 保存到知识库 → Markdown 写入 Vault → 可选图片本地化
```

### 5.5 摘录

```text
选中文字 → 摘录 → 如果文章尚未保存则先创建文章笔记 → 写入“我的摘录”
```

## 6. V1 页面

仅 3 个核心页面：

1. **今天**：按发布时间倒序的文章流；
2. **订阅**：来源列表与添加入口；
3. **阅读**：正文。

设置属于 Obsidian Plugin Setting Tab，不作为第四个主页面。

## 7. 今天页需求

### P0

- 默认展示最近文章；
- 标题；
- 来源；
- 发布时间；
- 轻量未读视觉；
- 点击进入阅读；
- 下拉/按钮手动刷新；
- 分页或 cursor 加载，首屏最多 30 条；
- 不展示摘要；
- 不展示封面大卡片（可在未来评估）；
- 不显示夸张未读数字。

### 排序

默认：`publishedAt DESC, id DESC`。

对于没有可靠发布时间的 Feed：使用 `fetchedAt`，并在模型中保留时间来源字段。

## 8. 订阅页需求

### P0

- 按类型分组：微信公众号、RSS；
- 来源名称；
- 可选头像/图标；
- 最近同步时间；
- 错误状态；
- 删除订阅；
- 手动刷新单个源；
- 添加入口；
- 添加时自动判断输入类型。

### 添加输入

支持：

- `https://...feed.xml`
- `https://...atom.xml`
- 普通网站 URL（RSS provider 可做 feed discovery）
- `https://mp.weixin.qq.com/...`

不支持公众号名称直接全网搜索作为 P0。

## 9. 阅读页需求

### P0

- 响应式布局；
- 桌面正文最大宽度 720px；
- 手机左右边距 20px（允许 CSS clamp）；
- 默认正文字号 17px；
- 行高约 1.8；
- 图片自适应；
- 图片点击大图；
- 链接可打开外部浏览器；
- 阅读进度恢复；
- 读到接近底部自动已读；
- `···`：原文、保存、字体设置；
- 下滚时顶栏弱化/隐藏，上滚恢复；
- 细进度条仅滚动时出现；
- 不出现摘要、AI、标签推荐、相关文章。

## 10. 阅读设置

V1 仅提供：

- 字号：小 / 标准 / 大，对应 16 / 17 / 19px；
- 行距：紧凑 / 舒适 / 宽松，对应 1.65 / 1.8 / 1.95；
- 宽度：窄 / 标准 / 宽，对应 640 / 720 / 820px；
- 跟随 Obsidian 浅色/深色主题。

不要实现独立主题商城、字体下载或联网字体。

## 11. 已读规则

满足任一：

- 用户滚动到正文高度的 >= 85%；
- 用户停留 >= 30 秒且滚动到 >= 70%；
- 用户手动标记。

列表点击本身不立即标记已读。

## 12. 阅读进度

本地保存：

```ts
interface ReadingState {
  articleId: string;
  read: boolean;
  progress: number; // 0..1
  scrollAnchor?: string; // 可选 block id
  updatedAt: string;
}
```

优先以 block anchor + 相对偏移恢复，无法恢复时使用比例。

## 13. 保存需求

默认目录：

```text
Feed/
├── 微信公众号/<source-name>/
└── RSS/<source-name>/
```

目录可在设置修改。

默认保存：完整正文 + frontmatter + 空白“我的笔记” + “我的摘录”。

图片默认：下载到 Vault；允许设置为远程链接。

## 14. 空状态与错误体验

### 今天无文章

显示：“还没有新文章。添加一个订阅，或刷新现有来源。”

### 服务端离线

列表保留已有 UI 状态，不清空；显示非阻塞提示：“无法连接 Feed Server”。

### 微信授权失效

订阅页源状态：“公众号采集服务需要重新授权”。提供“查看服务状态”，不在插件里伪装成功。

### 正文解析失败

阅读页显示已有元数据和：

> 这篇文章的正文暂时无法稳定解析。你可以打开原文阅读。

必须提供“打开原文”。禁止显示半截正文却声称完整。

## 15. 非功能目标

### 性能

- 今天页首屏 API P95 < 500ms（本地网络、自托管正常负载）；
- 插件打开已有缓存 UI < 300ms；
- 文章文本正文首次渲染 < 300ms（正文 API 返回之后）；
- 图片懒加载；
- 一次不把所有文章全文拉进插件。

### 可用性

- 服务端重启不丢订阅；
- 单个坏 Feed 不阻塞其他 Feed；
- 单个微信来源 429 不阻塞 RSS；
- 保存 Markdown 的失败不能影响阅读状态。

## 16. 成功标准

V1 成功不以“功能数量”衡量，而以：

- 可以持续订阅；
- 微信来源失败时可解释；
- 阅读页面干净；
- 手机端不别扭；
- 保存的 Markdown 可长期独立存在；
- 不依赖 AI 或特定一个微信采集实现。
