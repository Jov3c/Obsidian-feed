# Obsidian Feed — V1 后路线（非 V1 范围）

本文件用于防止开发时“顺手加功能”。以下都必须等 V1 验收后单独设计。

## V1.1 候选

- OPML import/export；
- 更强 RSS feed discovery；
- 收藏/稍后读；
- 来源自定义排序；
- 阅读快捷键；
- 更好的离线最近文章缓存；
- saved article index。

## V1.2 候选

- 可选系统通知（Bark/ntfy/webhook），由 server 触发；
- 多 Adapter（WechRss、WeWe-compatible）；
- 普通网页稍后读；
- Reader typography 高级设置。

## V2 候选

只有用户明确需要才讨论：

- 多设备阅读状态同步；
- 用户账号；
- server-side search；
- 标签；
- 全文索引。

## 明确不自动加入

AI/摘要不是“自然下一步”。如果未来要加，必须重新回答：为什么用户需要、是否破坏阅读定位、是否默认关闭。
